name: sr_edunova
description: "SR EduNova student app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  razorpay_flutter: ^1.3.7
  video_player: ^2.9.1
   
  url_launcher: ^6.3.0
  webview_flutter: ^4.13.1
  youtube_player_iframe: ^5.1.2
 # check latest version on pub.dev[web:289]

  # HTTP client
  dio: ^5.9.2

  # UI / misc
  cupertino_icons: ^1.0.8

  # (Optional) state management – you can keep or remove these for now
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

